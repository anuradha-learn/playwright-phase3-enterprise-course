// import { test } from '@playwright/test';

// test('File1 - Test1', async ({}, testInfo) => {
//     console.log(`Worker ${testInfo.workerIndex} → File1 - Test1`);
//     await new Promise(resolve => setTimeout(resolve, 5000)); // Simulate some async work
// });

// test('File1 - Test2', async ({}, testInfo) => {
//     console.log(`Worker ${testInfo.workerIndex} → File1 - Test2`);
//     await new Promise(resolve => setTimeout(resolve, 5000)); // Simulate some async work
// });